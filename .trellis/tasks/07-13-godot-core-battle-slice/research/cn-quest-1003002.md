# CN Quest 1003002 Evidence

## Source set

- CN resource extraction: `/home/codex/work/wf-assets-cn`, version `1.4.54`, revision `0f6b645bb27f9e544982648066f0c8f5e0184132`.
- Supplied client: `/home/codex/work/client-v1.8.1.apk` for enemy HP/ATK curve tables.
- Structural reference: CN `2.1.125` ActionScript decompile.
- Generated fixture: `godot/content/fixtures/quest_1003002.json`.
- Fixture SHA-256: `80eaf928533f5f7c6dcf722b8d4c7376f3fc75ffa70a9893fd27db70a87c4918`.

## Canonical graph

- Quest: `1003002`, `阳光透落的森林`, enemy level 25, stamina 7.
- Field data: `main_1_3_2`; field: `tree_forest`.
- Terrain logical asset: `battle/terrain/main_quest/chapter_01/main_chapter_01_03_02`.
- Terrain salted hash: `ad/a2cc23fd1230641ddd03816195b22be8b87009`.
- Zone 0: defeat 20 zakos.
  - emitter 0: `slango`, 60-frame post-death interval.
  - emitter 1: `fox`, 120-frame post-death interval.
  - emitter 2: `one_eyed_rabbit`, 150-frame post-death interval.
- Zone 1: general boss `fox`.

## Enemy/action evidence

- Level-25 `slango` zako: 284 HP / 37 ATK.
- Level-25 `fox` zako: 1707 HP / 47 ATK.
- Level-25 `one_eyed_rabbit` zako: 2371 HP / 32 ATK.
- Level-5 `fox` funnel: 202 HP / 8 ATK.
- Level-25 `fox` general boss: 33911 HP / 51 ATK.
- Fox zako shot: five-way 0.9x.
- Rabbit zako shot: single 1.0x cannon.
- Fox normal boss shot: seven-way 1.0x.
- Fox skill: six projectile patterns in three waves:
  - frame 0: seven-way + two-way, 1.1x;
  - frame 12: seven-way + two-way, 1.1x;
  - frame 24: seven-way + two-way, 1.1x.
- Fox funnel action spawns a level-5 Fox funnel.

## Fox state machine and terrain fallback

- Routine: `fox`; difficulty: `1`; initial state: `neutral1`.
- Complete unconditional cycle: 37 states.
- `skill1_charge1` and `skill1_charge2` use `TimeVariable("charge1")`; level 25 resolves to 300 frames.
- Move states reference `p0` through `p4`.
- The canonical terrain AMF3 remains unavailable. Existing `p1`/`p2`/`p3` fallback values remain byte-identical for earlier fixtures, while `p0`/`p4` are added only for fixtures whose state graph references them. Every move target must resolve at fixture validation time.

## Original Wait timing evidence

The decompiled Action DSL runtime confirms delayed commands are not action-start snapshots:

- `ListeningEvent.as` initializes `frameCount = -1`, stores the original `Environment`, and evaluates a `Wait` body when `frameCount == waitFrames`.
- `ActionEvaluator.as` resolves `CreateHitArea` coordinate systems and subjects from that environment when the delayed command executes.

Godot therefore stores delayed enemy runtime records by owner serial and resolves the owner's current position plus the player's current position on the due frame. Owner death, funnel destruction, zone teardown, and terminal teardown cancel the matching pending events. This differs intentionally from delayed player skills, which snapshot their target serial set at cast time according to the existing player-skill contract.

## Progression boundary

Clearing `1003002` advances to story `1004001` (`森之精灵`). Completing that story exposes unconverted battle `1004002`, which remains explicitly blocked while replay selects cleared fixture `1003002`.

## Verification

- Core converter suite: 7/7; checked fixtures `1001002`, `1002001`, and `1002002` remain byte-identical.
- Godot headless suite: `PASS 624 assertions` independently in each of two clean user-data roots.
- Normal flipper/skill input clears `1003002` with enemy actions enabled.
- Four-battle main-scene E2E passed, covering stamina 7, a distinct random run ID, result persistence, reload, story `1004001`, explicit blocking at `1004002`, and latest-cleared replay selection.
- Godot editor scan and 120-frame native main-scene smoke passed.
- Server-security and gacha-position regressions passed; the runtime dependency audit passed.
- Final review found and fixed same-step delayed-event decrementing and malformed `Wait` coercion; integrated frame-11/12 timing, non-collinear dynamic aiming, exact event shape, type, and nested-overflow regressions now cover them.

Exact forest terrain/spawn coordinates, the original 90-frame spawn animation, and original presentation assets remain explicit parity gaps.
